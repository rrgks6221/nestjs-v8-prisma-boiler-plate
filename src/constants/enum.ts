export enum ModelName {
  User = 'user',
  Post = 'post',
}

export enum SortOrder {
  Desc = 'desc',
  Asc = 'asc',
}

export enum BooleanString {
  True = 'true',
  False = 'false',
}

export enum ApiVersion {
  One = '1',
}
